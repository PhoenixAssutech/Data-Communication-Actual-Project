# Data-Communication-Actual-Project
This is the actual project, I made a mistake with the first solution.
if you are using linux or macOs do run "sudo apt install openssh-server"
"sudo service ssh start"
